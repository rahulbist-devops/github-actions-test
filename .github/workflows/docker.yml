name: Docker
on: push

jobs:
  testing-docker-action:
    runs-on: poc-custom-runner-eks
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/hello-docker
        id: hello
        with:
          who-to-greet: "World"
      - run: |
          echo "Time: ${{ steps.hello.outputs.time}}"
          echo $Hello